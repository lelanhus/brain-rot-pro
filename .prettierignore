# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
.svelte-kit/
build/
.vercel/
convex/_generated/

# Local, git-ignored deployment & editor state (not source)
.convex/
.claude/settings.local.json

# Vendored agent-skill packages (installed via `npx convex ai-files install`) —
# not our source; formatting them is churn that reinstalls overwrite.
.agents/
.claude/skills/
skills-lock.json

# Subagent-driven-development scratch (briefs, reports, ledger) — not source.
.superpowers/
